# This is the directory in the docker container where we will put the
# static files and where nginx will look for them.
NEEMS_STATIC_DIR=/app/dist

# If you're using `dosh test` to run the docker container, you can set
# this to tell nginx where it should send `/api` requests.
# NGINX_API_TARGET=${NGINX_APIT_TARGET:-http://host.docker.internal:8000}